services:
  - type: web
    name: web2knowledge
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: ANAKIN_API_KEY
        sync: false
